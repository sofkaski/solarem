'use strict';

describe('solaremFE.relayStateView module', function() {

  beforeEach(module('solaremFE.relayStateView'));

  describe('relayStateView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var relayStateViewCtrl = $controller('relayStateViewCtrl');
      expect(relayStateViewCtrl).toBeDefined();
    }));

  });
});